<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="author" content="NOZAKI">
    <meta name="description" content="リジョブのトップページです">
    <meta name="keywords" content="リジョブ,求人,美容">
    <title>野崎研修リジョブ</title>
    <SCRIPT LANGUAGE="JavaScript">
<!--
function menuLink(linkLoc)
{ if(linkLoc !="") { window.location.href=linkLoc;} }


function start(){
x=location.hash;
if(x!=""){
y=form1.select1.options;
for(i=0;i<y.length;i++){
z=y[i].value.indexOf(x,0)
if(z!=-1){
form1.select1.options[i].selected=true;
}
}
}

//-->
</SCRIPT>
  </head>
  <body onload="start()">


      <h1>案件</h1>
        <a href="job">案件詳細へ</a>

        <p>検索したい都道府県を選択してください。</p>

　　



  <form  action="list" method="get">
  <select name="area">

    <option selected>▼都道府県選択</option>
      <% Area.all.each do |area| %>
      <!-- オブジェクト.each do |変数| -->
      <!-- <a href="/search?area= -->
    <option value=<%= area.area_name %>><%= area.area_name %><br></option>
      <% end %>
  </select>
    <button type="search" name="button"
      style="background-color:peachpuff;width:200px;height:28px;"
      onmouseover="this.style.background='#FF1493'"
      onmouseout="this.style.background='#FFB6C1'">検索！</button>
  </form>


<div>
  <%= @area %>の案件は<%= @jobs.count %>件です<br>
  <% @jobs.each do |job| %>
    <a href="/job/<%= job.id %>">『<%= job.title %>』</a><br>
    →<%= job.description %><br><br>
  <% end %>
</div>





<% Area.all.each do |area| %>
<a href="/list?area=<%= area.area_name %>"><%= area.area_name %></a><br>
<% end %>

  </body>
</html>
