<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>新規作成</title>
    <!-- 透かし文字をvalue属性で設定する -->
  </head>
  <body>

    <%= form_for @new_edit, :url => {:action => 'confirm'} do |f| %>


     <form action="confirm" class="new_edit" id="new_edit" method="post">
       <input name="authenticity_token" type="hidden" value="1234" />


       <div class="field">
         <%= f.label :title, "名前" %>
         <%= f.text_field :title %>
       </div>

       <div class="field">
         <%= f.label :description, "説明文" %>
         <%= f.text_field :description %>
       </div>
       <div class="field">
         <%= f.label :area, "都道府県" %>
         <%= f.text_field :area %>
       </div>
       <div class="actions">
         <%= f.submit "登録する" %>
       </div>
</form>
  <% end %>
  </body>
</html>
